import { defineConfig } from 'vite'
import { svelte } from '@sveltejs/vite-plugin-svelte'

export default defineConfig({
  plugins: [svelte()],
  base: '/east-africa-bird-quiz/' // <-- set to '/<repo-name>/'
})